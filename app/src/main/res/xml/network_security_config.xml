<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!--
      Dev-only: the hackathon backend is plain HTTP, reached at the dev machine's LAN IP
      (see BACKEND_URL in app/build.gradle.kts) and at the emulator-only 10.0.2.2 alias.
      Cleartext stays disabled for every other host.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.68.1.85</domain>
        <domain includeSubdomains="true">10.171.250.90</domain>
        <domain includeSubdomains="true">10.58.28.231</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
